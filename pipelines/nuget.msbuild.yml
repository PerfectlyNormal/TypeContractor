trigger:
  branches:
    include:
      - main
  paths:
    include:
      - TypeContractor.MSBuild/*

resources:
  repositories:
    - repository: templates
      type: git
      name: Infrastructure/INF_BuildTemplates

parameters:
  - name: version
    type: string
    default: auto

extends:
  template: nuget-template.yml@templates
  parameters:
    version: ${{parameters.version}}
    includeScopes: 'msbuild'
    project: 'TypeContractor.MSBuild/TypeContractor.MSBuild.csproj'
    releaseNotesFilename: 'release-notes-msbuild.md'

# See https://dev.azure.com/Hogia/Infrastructure/_git/INF_BuildTemplates?path=%2Fnuget-template.yml&version=GBmaster for all available parameters
